apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-practice-time-printer.fullname" . }}
  labels:
    {{- include "devops-practice-time-printer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-practice-time-printer.selectorLabels" . | nindent 4 }}
